# ROUTE BY 계정 관리 시스템 - 개발 계획서

## 프로젝트 개요
- **프로젝트명**: ROUTE BY 계정 관리 시스템 (NEWAMS)
- **기술 스택**: React 18 + Vite (클라이언트) / Express.js (서버) / JSON 파일 DB
- **브랜드 컬러**: #E81E1E (로고/접속하기 전용), #00d4aa (UI accent)

---

## 구현 완료 기능

### 1. 계정 관리 (CRUD)
- 계정 추가/수정/삭제
- 소프트 삭제 (삭제된 계정 팝업에서 복구/완전삭제)
- 비활성/활성 상태 관리 (확인 모달 포함)
- 검색형 드롭다운: 계정소유자, 그룹
- 멀티태그 선택: 유형, 태그 (⚙ 관리 버튼)

### 2. 연동계정
- 기존 등록된 계정을 검색형 드롭다운으로 선택
- 사이트명(아이디) 형식으로 노출

### 3. 로그인방법 자동분류
- 아이디 + 패스워드 → 일반로그인
- 연동계정 → 간편로그인

### 4. 인증정보
- 여러 개 추가 가능 (SMS문자, 카카오톡, 휴대폰OTP, 이메일, 공인인증서)
- 방법별 전용 입력 필드 자동 전환

### 5. 패스워드
- 입력/표시 시 **** 마스킹
- 눈 아이콘 토글로 표시/숨기기
- 복사 버튼

### 6. 서비스 관리
- 서비스 추가/수정/삭제/갱신/이용종료/재개
- 이용시작일 캘린더 선택
- 만료일 자동 계산 + D-day 표시
- 자동갱신/수동갱신 설정
- 만료 임박 알림 (7/14/30/60일 전)

### 7. 엑셀 기능
- **내보내기**: 현재 계정 데이터를 xlsx 다운로드
- **가져오기**: xlsx 업로드로 일괄 등록/수정
  - 동일 아이디+URL → 수정 (덮어쓰기)
  - 없으면 → 신규 추가
  - 새로운 소유자/그룹/유형/태그 자동 등록
  - 띄어쓰기 자동 제거
  - 유형/태그는 쉼표(,) 구분 멀티 입력

### 8. 필터 시스템
- 검색창 2개 (전체 검색)
- 소유자 드롭다운
- 서비스 여부 Y/N
- 그룹/유형/태그 버튼형 멀티선택 필터 (각 행 분리)
  - 그룹 선택 시 해당 그룹의 유형/태그만 노출
  - "전체" 기본값
- 이용종료 포함 토글
- 비활성 포함 토글

### 9. 접속 추적
- 접속하기 클릭 시 행 음영 처리 + 최근접속 시간 표시
- 접속음영: 서버 저장, 매일 00시 자동 초기화
- 최근접속 시간: 음영 초기화 시에도 유지
- 접속음영 초기화 버튼 (필터 영역 우측 하단)

### 10. UI/UX
- 다크 테마
- 헤더/탭/필터바 상단 고정
- 테이블 제목행 sticky 고정
- 소유자는 펼침 시에만 노출
- 비활성 계정 반투명 + 태그 표시
- ROUTE BY 브랜드 로고 (logo.png)

---

## 향후 계획

### Phase 2 - 고도화
- [ ] 서비스 관리 탭 개선
- [ ] 대시보드 (통계/차트)
- [ ] 사용자 권한 관리
- [ ] 데이터 암호화 (패스워드)
- [ ] 배포 환경 구성 (Docker/클라우드)

### Phase 3 - 확장
- [ ] 알림 시스템 (이메일/슬랙)
- [ ] 변경 이력 추적 (로그)
- [ ] 다중 사용자 동시 편집 개선
- [ ] 모바일 반응형
