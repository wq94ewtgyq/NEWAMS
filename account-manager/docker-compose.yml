version: '3.8'
services:
  account-manager:
    build: .
    container_name: account-manager
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3001
      - NODE_ENV=production
